import React from 'react';
import LoadingContainer from '../styles/Loading.style';

function Loading() {
	return (
		<LoadingContainer>
			<h1>Loading</h1>
		</LoadingContainer>
	);
}

export default Loading;
